export interface TributeRow {
  id: string
  memorial_id: string
  user_id: string
  author_name: string
  message: string
  created_at: string
}

export interface CondolenceRow {
  id: string
  obituary_id: string
  user_id: string
  author_name: string
  message: string
  created_at: string
}
